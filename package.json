{
  "name": "s3-stale-objects",
  "version": "0.0.1",
  "description": "fetch s3 objects older than a given date",
  "bin": "bin/run",
  "author": "Jeff Dickey",
  "license": "MIT",
  "cli-engine": {
    "commands": "./lib/commands",
    "hooks": {
      "prerun": ["foo", "bar"]
    }
  },
  "dependencies": {
    "cli-engine": "^4.1.3",
    "cli-engine-command": "^7.0.4"
  },
  "devDependencies": {
    "standard": "^10.0.3"
  }
}
